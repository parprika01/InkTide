using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneLoader : MonoBehaviour
{
    public async void LoadScene(Scene scene)
    {
        // UIManager.Instance.ShowUI("LoadingPanel", true);

        // await SceneManager.LoadSceneAsync(scene.ToString());
        // UIManager.Instance.CloseAllSceneUIs();

        // UIManager.Instance.HideUI("LoadingPanel");
    }
}
